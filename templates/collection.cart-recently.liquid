{%- layout none -%}
{%- paginate collection.products by 50 -%}
[
  {%- for candidate in collection.products -%}
    {%- render 'cart-upsell-product-metadata', candidate: candidate -%}{% unless forloop.last %},{% endunless %}
  {%- endfor -%}
]
{%- endpaginate -%}

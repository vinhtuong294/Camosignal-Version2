{% comment %}
	------------------------------------------------------------------------------
	Section: Article template
	------------------------------------------------------------------------------
{% endcomment %}

{%- style -%}
	#shopify-section-{{- section.id -}} {
		padding-top: {{- section.settings.offset_top_mobile -}}px;
		padding-bottom: {{- section.settings.offset_bottom_mobile -}}px;
	}

	@media screen and (min-width: 768px) {
		#shopify-section-{{- section.id -}} {
			padding-top: {{- section.settings.offset_top_desktop -}}px;
			padding-bottom: {{- section.settings.offset_bottom_desktop -}}px;
		}
	}
{%- endstyle -%}

{%- assign container 					  = section.settings.container_width -%}
{%- assign product_list 					  = section.settings.product_list -%}
{%- assign enable_color_scheme_inheritance = section.settings.enable_color_scheme_inheritance -%}
{%- assign color_scheme 					  = section.settings.color_scheme -%}
{%- assign article_image 		     	  = article.image | default: settings.article_default_image -%}
{%- assign article_date_example            = 'May 25, 2024' -%}
{%- assign article_author                   = article.author | default: shop.name -%}
{%- assign article_words                    = article.content | strip_html | split: ' ' | size -%}
{%- assign article_read_time                = article_words | plus: 199 | divided_by: 200 -%}
{%- assign article_category                 = article.tags.first -%}

{%- if article_read_time < 1 -%}
	{%- assign article_read_time = 1 -%}
{%- endif -%}

{%- if container == 'inherit' %}
	{%- assign container_width = settings.container_width -%}
{%- else -%}
	{%- assign container_width = container -%}
{%- endif -%}

{%- if enable_color_scheme_inheritance -%}
	{%- assign color_scheme = settings.main_color_scheme -%}
{%- endif -%}

{%- render 'adastra' with '/src/styles/article.scss' -%}

{%- capture styles -%}
	--container-width: {{ container_width }}; {{-  -}}
	--inner-offset-top: {{- section.settings.inner_offset_top -}}px; {{-  -}}
	--inner-offset-bottom: {{- section.settings.inner_offset_bottom -}}px; {{-  -}}
	--inner-offset-top-mobile: {{- section.settings.inner_offset_top_mobile -}}px; {{-  -}}
	--inner-offset-bottom-mobile: {{- section.settings.inner_offset_bottom_mobile -}}px; {{-  -}}
{%- endcapture -%}

<article class="article color-{{ color_scheme }}" style="{{ styles }}">
	<header class="article-banner container">
		<div class="article-banner__heading-block js-animate {% if request.visual_preview_mode or section.index == 1 %}animated{% endif %}">
			<nav class="article-banner__breadcrumbs" aria-label="Article breadcrumb">
				<a href="{{ blog.url }}">{{ blog.title }}</a>

				{%- if article_category != blank -%}
					<span aria-hidden="true">/</span>
					<a href="{{ blog.url }}/tagged/{{ article_category | handleize }}">{{ article_category }}</a>
				{%- endif -%}
			</nav>

			<h1 class="article-banner__title">
				{{- article.title -}}
			</h1>

			{%- if article.excerpt != blank -%}
				<p class="article-banner__excerpt">
					{{- article.excerpt | strip_html -}}
				</p>
			{%- endif -%}

			<div class="article-banner__meta" aria-label="Article details">
				<span>Posted</span>
				{%- if article != blank -%}
					{{- article.published_at | time_tag: format: 'date' -}}
				{%- else -%}
					<time>{{ article_date_example }}</time>
				{%- endif -%}
				<span>by {{ article_author }}</span>
				<span aria-hidden="true">•</span>
				<span>{{ article_read_time }} min read</span>
			</div>

			<div class="article-banner__socials" aria-label="Share this article">
				{%- render 'share-button', social: "facebook", product: article -%}
				{%- render 'share-button', social: "twitter", product: article -%}
				{%- render 'share-button', social: "pinterest", product: article -%}
			</div>
		</div>

		{%- if article_image != blank -%}
			<figure class="article-banner__image-container">
				<picture>
					<source
						srcset="{{ article_image | image_url: width: 375 }} 1x,
								{{ article_image | image_url: width: 750 }} 2x"
						media="(max-width: 375px)"
					/>

					<source
						srcset="{{ article_image | image_url: width: 420 }} 1x,
								{{ article_image | image_url: width: 840 }} 2x"
						media="(max-width: 420px)"
					/>

					<source
						srcset="{{ article_image | image_url: width: 575 }} 1x,
								{{ article_image | image_url: width: 1150 }} 2x"
						media="(max-width: 575px)"
					/>

					<source
						srcset="{{ article_image | image_url: width: 768 }} 1x,
								{{ article_image | image_url: width: 1536 }} 2x"
						media="(max-width: 767px)"
					/>

					<source
						srcset="{{ article_image | image_url: width: 1200 }} 1x,
								{{ article_image | image_url: width: 2400 }} 2x"
						media="(max-width: 1199px)"
					/>

					<img
						src="{{ article_image | image_url: width: 1600 }}"
						srcset="{{ article_image | image_url: width: 1600 }} 1x,
								{{ article_image | image_url: width: 2400 }} 2x"
						alt="{{ article_image.alt | default: article.title | escape }}"
						class="article-banner__image js-animate {% if request.visual_preview_mode or section.index == 1 %}animated{% endif %}"
						width="{{ article_image.width }}"
						height="{{ article_image.height }}"
						loading="eager"
						fetchpriority="high"
					/>
				</picture>

				{%- if section.settings.show_image_caption and article_image.alt != blank -%}
					<figcaption>{{ article_image.alt }}</figcaption>
				{%- endif -%}
			</figure>
		{%- endif -%}
	</header>

	<div class="article__content container">
		<div class="article__inner">
			<section class="article-main">
				{%- if article.content != blank -%}
					<div class="article-main__rte">
						<div class="rte">
							{{- article.content -}}
						</div>
					</div>
				{%- endif -%}

				{%- if product_list != blank -%}
					<div class="article__products-swiper swiper js-article-products-swiper"
						 data-products-size="{{ product_list.count }}">
						<div class="article__swiper-heading-wrapper">
							<h4>{{- 'blogs.article.products_from_this_article' | t -}}</h4>

							<div class="article__swiper-buttons">
								<button class="swiper-button-prev-new js-article-swiper-button-prev">
									{%- render 'icons', icon: 'arrow-left' -%}
								</button>

								<button class="swiper-button-next-new js-article-swiper-button-next">
									{%- render 'icons', icon: 'arrow-right' -%}
								</button>
							</div>
						</div>

						<div class="article__products-swiper-wrapper swiper-wrapper">
							{%- for product in product_list -%}
								{%- assign image = product.featured_image -%}

								<div
									class="article__product swiper-slide js-product-card" {%- if product != blank %} data-existing-product{% endif %}>
									{%- render 'product-card-mini', product: product, is_bordered: true -%}
								</div>
							{%- endfor -%}
						</div>
					</div>
				{% endif %}

				<div class="article__share-buttons">
					<p>{{- 'blogs.article.share' | t -}}</p>

					<div class="article__socials">
						{%- render 'share-button', social: "facebook", product: article -%}
						{%- render 'share-button', social: "twitter", product: article -%}
						{%- render 'share-button', social: "pinterest", product: article -%}
					</div>
				</div>
			</section>

			{%- render 'comment-form' -%}
		</div>

	</div>

	{%- if blog.articles_count > 1 -%}
		<section class="article-read-also container">
			<div class="article-read-also__slider swiper js-article-read-also-swiper" data-articles-size="{{ blog.articles.size }}">
				<div class="article__swiper-heading-wrapper">
					<h2>{{ "blogs.article.read_also" | t }}</h2>

					<div class="article-read-also__buttons">
						<a href="{{ blog.url }}" class="button-link focus-visible-outline">
							<span data-text="{{ "blogs.article.see_all" | t: title: blog.title | escape }}">
								<span>{{ "blogs.article.see_all" | t: title: blog.title | escape }}</span>
							</span>
						</a>

						<div class="article__swiper-buttons">
							<button class="swiper-button-prev-new js-article-swiper-button-prev">
								{%- render 'icons', icon: 'arrow-left' -%}
							</button>

							<button class="swiper-button-next-new js-article-swiper-button-next">
								{%- render 'icons', icon: 'arrow-right' -%}
							</button>
						</div>
					</div>
				</div>

				<div class="article-read-also__slider-wrapper swiper-wrapper">
					{%- for blog_article in blog.articles -%}
						{%- if article.id != blog_article.id -%}
							<div class="article-read-also__slide swiper-slide">
								{%- render 'article-card',
									article: blog_article,
									aspect_ratio: '86.49%',
									size_width_420: 333,
									size_height_420: 333,
									size_width_575: 457,
									size_height_575: 457,
									size_width_767: 610,
									size_height_767: 610,
									size_width_991: 320,
									size_height_991: 320,
									size_width_1199: 378,
									size_height_1199: 327,
									size_width_1500: 325,
									size_height_1500: 281,
									size_width: 430,
									size_height: 372
								-%}
							</div>
						{%- endif -%}
					{%- endfor -%}
				</div>
			</div>
		</section>
	{%- endif -%}
</article>

<script type="application/ld+json">
  {{ article | structured_data }}
</script>

{%- render 'adastra' with '/src/scripts/article-template.js' -%}

{% schema %}
{
	"name": "Article main",
	"settings": [
		{
			"type": "checkbox",
			"id": "enable_color_scheme_inheritance",
			"label": "Enable color scheme inheritance",
			"default": true,
			"info": "When this setting is enabled, the color scheme is inherited from the theme settings."
		},
		{
			"type": "color_scheme",
			"id": "color_scheme",
			"label": "Color scheme",
			"default": "scheme-1"
		},
		{
			"type": "checkbox",
			"id": "show_image_caption",
			"label": "Show featured image caption",
			"default": true
		},
		{
			"type": "header",
			"content": "Products from article"
		},
		{
			"type": "product_list",
			"id": "product_list",
			"label": "Products",
			"limit": 8
		},
		{
			"type": "header",
			"content": "Layout"
		},
		{
			"type": "select",
			"id": "container_width",
			"label": "Container width",
			"options": [
				{
					"value": "1808px",
					"label": "Default - 1680px"
				},
				{
					"value": "2048px",
					"label": "Large - 1920px"
				},
				{
					"value": "100%",
					"label": "Full width"
				},
				{
					"value": "inherit",
					"label": "Inherit of general setting"
				}
			],
			"default": "inherit",
			"info": "if you select 'Inherit of general setting', then container width will be used from the Theme settings/Layout"
		},
		{
			"type": "paragraph",
			"content": "External offsets"
		},
		{
			"type": "range",
			"id": "offset_top_desktop",
			"min": 0,
			"max": 100,
			"step": 1,
			"unit": "px",
			"label": "Desktop external offset top",
			"default": 40
		},
		{
			"type": "range",
			"id": "offset_bottom_desktop",
			"min": 0,
			"max": 100,
			"step": 1,
			"unit": "px",
			"label": "Desktop external offset bottom",
			"default": 40
		},
		{
			"type": "range",
			"id": "offset_top_mobile",
			"min": 0,
			"max": 100,
			"step": 1,
			"unit": "px",
			"label": "Mobile external offset top",
			"default": 25
		},
		{
			"type": "range",
			"id": "offset_bottom_mobile",
			"min": 0,
			"max": 100,
			"step": 1,
			"unit": "px",
			"label": "Mobile external offset bottom",
			"default": 25
		},
		{
			"type": "paragraph",
			"content": "Internal offsets"
		},
		{
			"type": "range",
			"id": "inner_offset_top",
			"min": 0,
			"max": 100,
			"step": 1,
			"unit": "px",
			"label": "Desktop internal offset top",
			"default": 25
		},
		{
			"type": "range",
			"id": "inner_offset_bottom",
			"min": 0,
			"max": 100,
			"step": 1,
			"unit": "px",
			"label": "Desktop internal offset bottom",
			"default": 25
		},
		{
			"type": "range",
			"id": "inner_offset_top_mobile",
			"min": 0,
			"max": 100,
			"step": 1,
			"unit": "px",
			"label": "Mobile internal offset top",
			"default": 25
		},
		{
			"type": "range",
			"id": "inner_offset_bottom_mobile",
			"min": 0,
			"max": 100,
			"step": 1,
			"unit": "px",
			"label": "Mobile internal offset bottom",
			"default": 25
		}
	]
}
{% endschema %}
